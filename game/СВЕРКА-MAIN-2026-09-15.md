# Сверка main и origin/main — 15.09.2026

Результат сверки перед новым кодовым этапом (правило «перед новым кодом — сверка версий»). Слияние ещё **не выполнено**: план ниже ждёт подтверждения автора.

## Состояние

- Общая база: `1208222` (12.09, «main запушен в drakelovc2/lmkotiki»).
- Локальный `main`: `a7937e3` — 4 коммита (этап 1 технически готов 12.09; синхронизация снимка `hoplite/olous-f7d7bb27` 12–13.09; записи по ачивкам 15.09).
- `origin/main`: `fca76c1` — 52 коммита, из них рабочих: merge PR #1 из `hoplite/lato-1ad609bf` (этапы 1A–1F от сессии 11.09) и веб-загрузки автора «Add files via upload», которыми контент перезалит из путей `Desktop/глмкотики/…` в корень репозитория.
- Репозиторий локально укоренён в `/home/deck` (весь домашний каталог), поэтому локальные пути имеют префикс `Desktop/глмкотики/`, а на origin — без префикса. Это различие путей, не содержимое.

## Сравнение содержимого (пути нормализованы)

- Общих файлов: 628, из них идентичных: 613.
- Расходятся содержимым: 15 файлов — и в каждом локальная версия является более поздним надмножеством origin (у 4 из 6 код-файлов origin-версии найдены как предки в истории локальной ветки; у `stage1_ui.gd` и `tests/stage1_ui.gd` diff не содержит ни одной удалённой строки, то есть локальная версия включает origin целиком).
  - Код: `game/3d-probe/probes/stage1_terrain.gd`, `probes/stage1_ui.gd`, `project.godot`, `scripts/geometry.gd`, `tests/stage1_ui.gd`, `README.md`.
  - Документы: `CONTEXT.md`, `LOCAL_CONTEXT.md`, `MODELS.md`, `game/AGENTS.md`, `game/HOPLITE_CONTEXT.md`, `ideas/common/systems/achievements/plan.md`, `ideas/main-story/lore/events/README.md`, `ideas/requests/user-request.md`, `implementation-plan/prologue/control/РЕЕСТР-АССЕТОВ.md`.
- Только локально: 32 файла — работа olous-сессии (пещерная проба и её тесты, шрифты PT Sans/Yeseva One, текстуры AmbientCG rock023, фон меню, `ИСТОЧНИКИ-АССЕТОВ.md`, план Заброшки).
- Только на origin: 7 файлов — служебные (`game/tests/__pycache__/*.pyc`, `game/Текстовий файл….txt`, `agent-browser.json`, 4 фото). Копии фото и `agent-browser.json` есть в рабочей копии без трекинга.

## План объединения (один шаг, без потерь)

1. `git merge origin/main` в локальный `main`.
2. Все 15 конфликтов содержимого разрешить стороной **локальной** («ours») — она строго новее и включает origin; потеря origin-работы исключена проверкой выше.
3. Файлы «только на origin» принять как есть, но `game/tests/__pycache__/` исключить и добавить в `.gitignore` (pу-байткод не должен трекаться).
4. Прогнать полный headless-набор тестов, просмотреть diff, локальный коммит. Push — только по прямому запросу автора.
5. В HOPLITE_CONTEXT.md записать актуальный снимок (сделано этим же коммитом, см. блок «Известный снимок»).

Альтернатива, если автор предпочитает чистую историю: перебазировать 4 локальных коммита на `origin/main` с теми же разрешениями. Результат содержимого одинаковый.
