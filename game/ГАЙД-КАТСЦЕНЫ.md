# Гайд: как делать катсцены

Для любой модели, которая будет делать катсцены. Образец — стартовая катсцена С1: `game/3d-probe/scenes/cutscene/intro.tscn`, кадры — `game/3d-probe/evidence/cutscene-2026-09-25/`. Графика — по [гайду «как в меню»](ГАЙД-ГРАФИКА-КАК-В-МЕНЮ.md).

## 1. Сначала канон, потом постановка

1. Найди сцену в каноне: `ideas/prologue/story-full.md`, `ideas/FULL_PLAN.md`, карточка события в `ideas/`. Выпиши **только то, что сказал автор**: что происходит, где, сколько длится, какой текст на экране.
2. Всё, чего в каноне нет, — не придумывай. Реплики, имена, лишние события, чужие персонажи — только от автора. Если без текста нельзя — заглушка с пометкой `[заглушка]` и вопрос автору.
3. Нерешённое автором (например, кнопка пропуска) — тоже заглушка с комментарием в коде «ЗАГЛУШКА: автор ещё не решил».

Пример С1 — что взято из канона: оживлённая улица, машины в обе стороны, кот сходит в тропу, тропа сначала бетон, потом лесная, табличка «халявная клубника через 500 метров» (текст обновлён автором 25.09.2026; прежний — «халявная клубника через 500 метров») (текст дословно), крупный план таблички **3 секунды**, вся сцена **около минуты**, пропуск — сразу к управлению. Всё остальное (закат, фонари, ракурсы) — оформление, его можно менять.

## 2. Из чего состоит сцена катсцены

**Главный приём: мир катсцены — та же сцена, где потом играет игрок.** Стартовая катсцена включает `scenes/world/start_area_world.tscn` (улица, лес, табличка, машины), и игровая сцена `scenes/world/start_area.tscn` включает её же. Катсцена кончается в точке `SPAWN_Z` — ровно там кот появляется в игре. Так нет «склейки» между роликом и игрой, и мир правится в одном месте.

| Узел | Зачем |
|---|---|
| `World` (сцена мира) | Земля, лес, объекты, свет. Собирается своим сборщиком (`start_area_builder.gd`). |
| `Builder` (скрипт `*_builder.gd`) | Сборщик **постановки**: пишет анимацию. Мир не трогает. |
| `CatModel` | Модель кота `scenes/player/cat_variants/cat_<id>.tscn`. Шаг — свойство `move_speed` (ноги двигаются сами, в такт скорости), лицо — `emotion`. В `_ready()` катсцены модель заменяется на выбранную игроком: `CatModel.replace($CatModel, SaveGame.cat_variant)` — имя узла то же, анимация её находит. |
| `ShotCamera` | Одна камера, которую двигает анимация. |
| `AnimationPlayer` → анимация `intro` | **Вся постановка:** путь и поворот кота, `move_speed`, камера, затемнение. |
| `Overlay` | Чёрные полосы сверху/снизу, подсказка пропуска, `Fade` для затемнения. |
| Скрипт корня (`intro.gd`) | Только запуск анимации, пропуск и переход в игру. |

Живые мелочи (машины едут, фонари светят) — отдельные маленькие скрипты на своих объектах (`traffic_car.gd`), не в анимации.

## 3. Как построить новую катсцену (по шагам)

1. **Мир:** если катсцена идёт там, где игрок будет играть, — включи ту же сцену мира. Новое место — скопируй `start_area_world.tscn` + `start_area_builder.gd` + `start_area_terrain.gd`.
2. **Земля** — функции `height(x, z)` и `color(x, z, y)` в `*_terrain.gd`; меш строит общий `Mesher.height_field(...)`. Дороги, тропы, разметка — это просто цвет по координатам. Мелкие кубики (0,5) там, где смотрят вблизи, крупные (1,0) на склонах — иначе файл земли огромный.
3. **Объекты** — готовые виды из `scenes/style_probe/kinds/` (деревья, кусты, фонарь) или новые рецепты по образцу `street_recipes.gd` (машина, табличка, тоннель).
4. **Постановка:** скопируй `intro.tscn`, `intro.gd`, `intro_builder.gd`.
5. **Путь героя** — список точек в `_cat_path()`: `[позиция, стоит ли здесь]`. Время считается само по скорости `CAT_SPEED` — поэтому шаг никогда не «скользит».
6. **Планы камеры** — список `shots`: каждый план — ключи `[время, где камера, куда смотрит]`. Между ключами одного плана камера плавно едет; первый ключ плана сдвинут на 0,001 с — получается резкая склейка.
7. **Надписи на предметах** — `Label3D` на самом предмете (табличка), а не текст поверх экрана.
8. **Пересобрать, снять кадры, проверить** (раздел 5).

## 3а. Мимика в катсцене

Дорожка `CatModel:emotion` (дискретная) — ключи со значениями `neutral` / `joy` / `sad` / `angry` / `surprised`. Эмоцию нужно **показать планом на мордочку** (в С1 — `FACE_SHOT_SECONDS` после крупного плана таблички): со спины или издалека её не видно. Какая эмоция где — решает автор (сейчас в С1 радость у таблички — предложение).

## 4. Правила постановки

- **Около минуты — это около минуты.** Длина задаётся постановкой, не растягивается паузами.
- **Кот стоит — кот неподвижен** (канон). На остановке `move_speed = 0`; модель сама возвращается в покой.
- **Длительности из канона — точно.** Крупный план таблички — `SIGN_CLOSEUP_SECONDS = 3.0`; тест это проверяет.
- **Камера не заслоняется.** Деревья и трава не ставятся ближе 4,5 единицы к точкам камеры (`_near_camera`). Фонари тоже.
- **Герой и главный предмет в кадре не перекрывают друг друга:** ставь камеру сбоку, чтобы между ними было 20–30° угла.
- **Край мира не виден.** Земля и лес тянутся за край кадра; машины исчезают и появляются за кадром (`x_min`/`x_max`).
- **Нельзя опасно:** кот не выходит на проезжую часть (тест проверяет).
- **Пропуск обязателен** и ведёт туда же, куда конец катсцены.
- Начало — выход из чёрного, конец — уход в чёрное, затем переход в игру.

## 5. Команды

```bash
G=~/.cache/10000-metres-probe/godot-4.7.2-linux-x86_64/godot
cd game/3d-probe
export VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/lvp_icd.json

# Пересобрать мир и постановку (с окном — иначе трава MultiMesh сохранится пустой)
xvfb-run -a -s "-screen 0 1280x800x24" $G --path . -s res://tools/build_start_area.gd

# Кадры в нужные секунды
xvfb-run -a -s "-screen 0 1280x800x24" $G --path . --resolution 1280x800 -s res://tools/capture_cutscene.gd -- \
  res://scenes/cutscene/intro.tscn "$PWD/evidence/<папка>" 3 11 18.5 21.5 30 52

# Кадры концовки (--fixed-fps 60 обязателен: иначе при медленной отрисовке анимация пролетает за кадр)
xvfb-run -a -s "-screen 0 1280x800x24" $G --fixed-fps 60 --path . --resolution 1280x800 -s res://tools/capture_car_ending.gd -- "$PWD/evidence/<папка>"

# Проверки
$G --headless --fixed-fps 60 --path . -s res://tests/intro.gd
$G --headless --fixed-fps 60 --path . -s res://tests/start_area.gd
```

В редакторе Godot: открыть `intro.tscn` → узел `AnimationPlayer` → вкладка «Анимация» — все ключи видны и двигаются мышью. **Но** кнопка «Пересобрать катсцену» у `Builder` перезапишет анимацию: ручные правки ключей перенеси в `_cat_path()` / `shots`.

## 6. Ошибки, на которых уже обжигались

| Что видно | Причина | Как исправить |
|---|---|---|
| Камера «следит» за котом, которого нет в кадре | У новой `Animation` длина 1 с, и `position_track_interpolate` обрезает время | Сразу после `Animation.new()` задать большую длину, в конце — настоящую. Уже сделано в `_build_animation`. |
| Кадр закрыт фонарём или кроной | Объект у камеры | `_near_camera` для всего крупного. |
| Кот закрывает табличку | Камера смотрит на них с одной стороны | Камеру — сбоку, со стороны предмета. |
| Надпись не влезает в табличку | Большой `pixel_size` у `Label3D` | Уменьшить (`0.0031` для доски 3,2). |
| Машины исчезают посреди кадра | Граница петли в кадре | `x_min`/`x_max` за краем видимой земли. |
| Видно, где кончается земля | Земля короче, чем видит камера | Расширить `X_MIN`/`X_MAX` в `*_terrain.gd`. |
| Трава пропала | Пересборка с `--headless` | Пересобрать с окном. |
| Концовка на кадрах чёрная | Отрисовка медленная, анимации пролетают за один кадр | Снимать с `--fixed-fps 60`. |
| Камера не отдаляется в концовке | Камера внутри `SpringArm3D`, пружина возвращает её за кота | `camera.reparent(мир, true)` перед полётом (как в `car_ending.gd`). |
| В титрах «от себя» добавлен текст | Модель придумала | Только то, что дал автор, остальное — `[заглушка]`. |

## 6а. Концовки

Образец — `scenes/endings/car_ending.tscn` (С9-машина, «глупый суицидник»): полёт кота → отдаление камеры → «Конец» → титры → выход. Концовка — отдельная сцена-слой, игровая сцена вызывает `start(кот)`. На время концовки: управление выключено, пауза не открывается (`PauseMenu.enabled = false`), интерфейс спрятан. `quit_on_finish = false` — для проверок. Пропуск — сразу к титрам (канон).

## 7. Перед тем как сказать «готово»

- [ ] Всё, что в сцене происходит, есть в каноне; остальное — оформление или `[заглушка]`.
- [ ] Длительности из канона выдержаны (тест).
- [ ] Кадры сняты в 5–7 точках, каждый просмотрен: герой в кадре, ничего не заслоняет, край мира не виден.
- [ ] Пропуск работает и ведёт в игру; конец катсцены — туда же.
- [ ] `tests/intro.gd` (или тест новой катсцены) проходит.
- [ ] Автору — кадры и честный статус; «принято автором» — только после его ответа.
