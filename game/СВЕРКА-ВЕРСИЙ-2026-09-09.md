# Этап 0 — сверка версий и ручной план объединения — 09.09.2026

**Модель:** Freebuff/Buffy. Игровой код не менялся: только сверка, автоматические проверки и документация.
**Статусы ниже:** `проверено автоматически` (команда + вывод) либо `внешний факт` (сообщение автора). Авторских приёмок нет.

## 1. Фактический снимок этой машины

| Параметр | Значение | Статус |
| --- | --- | --- |
| Корень git-репозитория | `/home/deck` — репозиторием покрыта вся домашняя папка, проект лежит в `Desktop/глмкотики/` | проверено автоматически (`git rev-parse --show-toplevel`) |
| Ветка проекта | `main` (локальная ветка проекта), HEAD `459af7c80345f790c48e9e57a27953260db5c287`, 09.09.2026 00:58 +03, «котики: уточнение — „продолжить/рестарт“ после мини-игры, после сцены побега конец» | проверено автоматически |
| Указанная в карточке ветка | `hoplite/ambrakia-6fff6cab` — на этой машине отсутствует (нет ни локальной ветки, ни remote-ветки) | проверено автоматически (`git branch -a`) |
| Указанный SHA `030347c8…` | объекта нет в локальной базе объектов | проверено автоматически (`git cat-file -t`) |
| Указанный SHA игрового кода `3e1caea1…` | объекта нет в локальной базе объектов | проверено автоматически (`git cat-file -t`) |
| `origin` | `https://github.com/88hhaddos/fluffy-spork.git` — чужая история (ставки/баланс 09.07.2026), без общего merge-base с проектом; `origin/main` не является «свежим main» этого проекта | проверено автоматически |
| Рабочее дерево | нечистое: 33 изменённых отслеживаемых файла проекта + ~120 неотслеживаемых (в основном `game/3d-probe/` код и тесты, `.uid`, планы 09.09, фото) | проверено автоматически |
| Stash | пуст | проверено автоматически |
| Движок | Godot `4.7.2.stable.official.ed1daf0bf`, запускается как `.tools/godot/godot`; копия в `~/.cache/10000-metres-probe/` | проверено автоматически (`--version`) |
| Импорт проекта | `--editor --import --quit` — успешно, exit 0 | проверено автоматически |

Вывод: «сверка веток/SHA двух репозиториев» в этой среде технически невозможна — в объектной базе нет коммитов ветки Hoplite. Переданный снимок Hoplite существует здесь только как набор файлов в рабочем дереве поверх локального `main`.

## 2. Происхождение рабочего дерева (реконструкция по журналам)

- Последний локальный коммит с игровым кодом: `a88c8c7` 08.09.2026 (потом — только документация: `9f9c038`, `e2df748`, `547f9e0`, `459af7c` 08–09.09).
- Журнал Hoplite (`game/HOPLITE_CONTEXT.md`) фиксирует свою ветку и последний игровой SHA — их здесь нет; журналы `game/requests/user-request.md` и `ideas/requests/user-request.md` содержат одинаковые дословные сообщения автора 06–09.09 с обеих сторон.
- Значит, поверх локального `main` в рабочее дерево скопирован (не закоммичен) снимок работы Hoplite. Двусторонний merge тут не нужен: фактическая ситуация — «локальная база + внешний оверлей файлов».

## 3. Классификация изменённых файлов (сверка с обеими версиями)

33 отслеживаемых файла, все в проекте. По журналам Hoplite затрагивал `game/` только вчерне те же области — прямых сюжетных расхождений в контенте не найдено; расхождения оформленные и строчные (см. §4).

### 3.1. Только добавления Hoplite поверх локальной базы — принять как есть
`AGENTS.md`, `CONTEXT.md` (+17/−2, заменённые локальные строки сохранены в git-истории), `game/3d-probe/scripts/cat.gd`, `location_forest.gd`, `probe.gd` (+788), `probe_input.gd`, `probe_inventory.gd`, `dialogue_data.gd`, `dialogue_box.gd`, `inventory_art.gd`, `inventory_hud.gd`, `full_map_hud.gd`, `world.gd`, `route.gd` (метры 40/500 по решению автора 09.09), `game/3d-probe/tests/*.gd` (dialogue, location_forest, prologue_start, route500), `game/CONTEXT.md`, `game/ПЛАН-ПРОЛОГА.md`, `game/requests/user-request.md` (+226), `ideas/requests/user-request.md` (+215), `ideas/prologue/story-full.md`, `ideas/prologue/story-skeleton.md`, `ideas/FULL_PLAN.md`, `ideas/README.md`, `implementation-plan/README.md`, `MODELS.md`, `game/3d-probe/README.md` (добавляет intro/prologue-тесты, обновляет счётчики).

Плюс ~120 неотслеживаемых файлов: K4–K9 код и тесты (`intro_*.gd`, `location_church.gd`, `npc_*.gd`, `stream_minigame.gd`, `pig_*.gd`, `vtuber_*.gd`, `fishing_*.gd`, `road_*.gd`, `fight_*.gd`, `mime_tutorial.gd`, `ending_screen.gd` и `.uid`), планы 09.09 (`game/ПЛАН-ДОРАБОТКИ-…`, `game/ПЛАН-ПРОЦЕССА-…`, `game/АУДИТ-ПРАВИЛ-…`, `game/HOPLITE_CONTEXT.md`, `implementation-plan/prologue/execution-2026-09-09.md`, `LOCAL_CONTEXT.md`), `ideas/prologue/car-ending.md`, фото.

### 3.2. Реальные пересечения — ручной план объединения (не перезаписывать автоматически)

**3.2.1. `.gitignore` — единственное содержательное расхождение.**
Локальная версия: `.tools/`, `.hoplite/`, `.freebuff/`, `.zcode/`, `agent-browser.json`, `photo_*.jpg`, `Текстовий файл….txt`, `game/3d-probe/build/*` + `!game/3d-probe/build/.gdignore`, `__pycache__/`.
Hoplite: `.hoplite/`, `game/3d-probe/.godot/`, `game/3d-probe/build/`, `game/tests/__pycache__/`, `*.pyc`.
Риск: у Hoplite **не игнорируется `.tools/`** — туда ставится бинарник Godot (~100 МБ), сейчас он уже виден git'у как неотслеживаемый.

План объединения (ручной, единственный пересчёт строк):
```
.hoplite/
.freebuff/
.zcode/
.tools/
agent-browser.json
game/3d-probe/.godot/
game/3d-probe/build/
game/tests/__pycache__/
__pycache__/
*.pyc
photo_*.jpg
Текстовий файл….txt
```
То есть: сохранить весь локальный набор игноров + добавить полезное Hoplite (`.godot/`, `game/tests/__pycache__/`). Не игнорировать `game/3d-probe/evidence/` (исторические кадры документации — на момент записи не закоммичены, вопрос их коммита отдельный, автор не решал).

**3.2.2. `ideas/endless/plan.md` — удалено Hoplite, есть в git-истории локально.**
По журналу Hoplite: содержимое перенесено, по канону «отменённый номер/файл не переиспользуется вслепую». План: оставить удаление как есть (файл восстановим из истории `git show HEAD:…`), в комментариях ничего не писать. Автор при желании вернёт перенесённое из `ideas/main-story/endless/`.

**3.2.3. Заменённые строки в `AGENTS.md` / `CONTEXT.md` / `game/CONTEXT.md` / `game/ПЛАН-ПРОЛОГА.md`** — Hoplite обновил устаревшие формулировки (статусы, запреты, метры). Все заменённые локальные строки остаются в git-истории; содержательных локальных правок поверх Hoplite нет. План: принять версию рабочего дерева, ничего не переписывать обратно.

### 3.4. Требующие решения автора (не блокируют чтение)
- Закоммитить ли текущее состояние рабочего дерева как единый «снимок Hoplite» на `main` (или отдельную ветку) — это публикация, нужна явная команда автора.
- Коммитить ли фото `photo_*.jpg` и файлы `.freebuff/`/`.zcode/` — сейчас не игнорируются частично, коммитить их не планируется.
- `.uid`-файлы Godot обычно коммитят вместе со скриптами; решение автора не запрашивалось.

## 4. Автоматические проверки

Движок 4.7.2 stable. Команда профиля: `--headless --path game/3d-probe --fixed-fps 60 --script tests/<suite>.gd`.

**Полная регрессия 09.09.2026 — 29 игровых сюит, все зелёные (проверено автоматически, лог `/tmp/hoplite_regress_0909.log`):**

| Сюит | Проверок |
| --- | --- |
| run 34 · rendering 24 · needs 49 · route500 21 · menu 37 · location_forest 11 · prologue_start 18 · dialogue 27 · inventory 71 · inventory_proof 11 · probe_inventory 12 | 315 |
| stint 24 · bratishkin_stream 18 · pig_minigame 14 · pig_follow 15 · vtuber_room 14 · stint_report 11 · super_pig 10 · moderator_ending 17 | 123 |
| fishing 28 · road_encounters 32 · road_interactions 20 · road_dodge_events 23 · road_story_npcs 26 | 129 |
| mime_tutorial 13 · location_church 20 · npc_church 11 · church_beggar 9 · intro_sequence 60 | 113 |
| **Итого быстрые** | **680 проверок, 0 падений** |

- `joypad_guard.gd` — не сюит: это служебный помощник headless-тестов (`extends RefCounted`, `strip()` гасит живые joypad-события). Первая попытка запустить его как сюит ошибочна; включать его в регрессионные списки не нужно.
- `pacing` — пройден отдельно при `--fixed-fps 60`: **21 проверка, 0 падений** (лог `/tmp/pacing_fixed_0909.log`). Проход первого маршрута — 1801,500 симуляционных секунд (30:01,5) за 193,441 с по часам, минимальная высота 0,000; проход с нуждами — 1801,500 с ходьбы / 2401,550 с всего, 3 привала, 7 порций. Канонический темп 500 м ≈ 30 минут подтверждён; аппаратный замер FPS этим не является.

Импорт проекта `--editor --import --quit` — exit 0. Проверено автоматически; визуальной проверки нет, приёмок автора нет.

## 5. Статусы этапа 0 (по четырёхуровневой шкале)

- Сверка версий: `проверено автоматически` (факты §1–§3).
- Полная регрессия: `проверено автоматически` после завершения pacing (см. §4).
- Визуальная проверка: не выполнялась (этап 0 — документация; движок запускался headless).
- Принято автором: нет. Для продолжения нужен выбор автора (см. §6).

## 6. Что дальше (по плану исполнения)

1. Автор подтверждает объединение `.gitignore` по §3.2.1 (и, при желании, коммит снимка).
2. Этап 1 — короткие визуальные пробы (масштаб, три ширины тропы, камера, рельеф, материалы/CC0, интерфейс) — только после шлюза этапа 0. Ничего из этого не переносится на весь маршрут 500 м до выбора автора.
3. Открытые вопросы перед ближайшим кодом — в разделе 20 плана исполнения (кнопки пропуска, ширина тропы, камера, CC0-кандидаты).
